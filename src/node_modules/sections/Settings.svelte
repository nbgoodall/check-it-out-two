<script>
  import { project } from 'store'

  $: invalidTitle = $project.title.length == 0
</script>

<div class="flex justify-between">
  <input bind:value={ $project.title } required class="bg-transparent focus:outline-none focus:border-gray-500 border px-1 { invalidTitle && 'border-red-400 focus:border-red-600' }">

  <div class="flex text-gray-600">
    <button
      on:click={ () => $project.showUnderlay = !$project.showUnderlay }
      class="focus:outline-none border-2 border-gray-600 px-3 text-sm { $project.showUnderlay && 'bg-teal-500 text-white border-teal-500' }"
    >
      Underlay
    </button>

    <button disabled={ invalidTitle } on:click={ $project.exportGif } class="focus:outline-none bg-purple-600 text-white px-3 py-1 text-sm ml-3 { invalidTitle && 'opacity-50' }">Export GIF</button>
  </div>
</div>