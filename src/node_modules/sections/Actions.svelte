<script>
  import { project } from 'store'
</script>

<div class="flex justify-between">
  <div class="flex">
    <button class="focus:outline-none bg-blue-600 text-white px-3 py-1 text-sm" on:click={ $project.addFrame }>Add</button>
    <button class="focus:outline-none bg-orange-500 text-white px-3 py-1 text-sm ml-3 disabled:opacity-50" disabled={ $project.frames.length <= 1 } on:click={ $project.deleteFrame }>Delete</button>
    <button class="focus:outline-none bg-gray-400 px-3 py-1 text-sm ml-3" on:click={ $project.duplicateFrame }>Duplicate</button>
  </div>

  <div class="flex">
    <button class="focus:outline-none bg-black text-white px-3 py-1 text-sm" on:click={ $project.clearFrame }>Clear</button>
    <button class="focus:outline-none bg-red-600 text-white px-3 py-1 text-sm ml-3" on:click={ $project.reset }>Reset</button>
  </div>
</div>