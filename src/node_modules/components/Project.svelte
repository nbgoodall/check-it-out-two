<script>
  import { Frame } from 'models'

  export let project, setProject, deleteProject;
</script>

<div on:click={ setProject } class="group bg-gray-200 flex mt-4 p-4 hover:bg-gray-300 cursor-pointer justify-between items-center">
  <div class="flex">
    <div class="bg-white">
      <img src="{ project.image }" class="h-12 w-12" alt="First frame.">
    </div>

    <div class="ml-4 flex flex-col justify-between leading-none">
      <h3 class="text-xl">{ project.title }</h3>

      <small class=" text-gray-600 uppercase tracking-wide">{ project.frames_count } frame{ project.frames_count === 1 ? '' : 's' }</small>
    </div>
  </div>

  <button on:click={ deleteProject } class="pointer-events-auto hidden group-hover:block focus:outline-none bg-red-600 text-white px-3 py-1 text-sm ml-3">Delete</button>
</div>