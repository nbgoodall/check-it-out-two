<script>
  import { fade } from 'svelte/transition';

  export let visible = false
</script>

{#if visible }
  <div transition:fade="{{ duration: 200 }}" on:click={ () => visible = !visible } class="fixed inset-0 bg-black opacity-50 pointer-events-auto z-10"></div>

  <div transition:fade="{{ duration: 100 }}" class="fixed inset-0 pointer-events-none z-10 p-16 flex">
    <div class="bg-white w-full max-h-full max-w-xl p-8 pointer-events-auto m-auto overflow-scroll">
      <slot></slot>
    </div>
  </div>
{/if}