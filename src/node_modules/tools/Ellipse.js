import ShapeTool from './ShapeTool'

import { ellipse } from 'lib/drawing'

class Ellipse extends ShapeTool {
  constructor() {
    super()

    this.cursor = 'crosshair'

    this.name = 'ellipse'

    this.shapeFn = ellipse
  }
}

export default new Ellipse()