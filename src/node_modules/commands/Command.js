import { history } from 'store/history'

// Composite commands, too!!!

class Command {
  static perform() { throw new Error('Not implemented.') }

  static undo() { throw new Error('Not implemented.') }

  static redo() { throw new Error('Not implemented.') }

  static push(name, state) {
    return history.push(name, state)
  }
}

export default Command