import { get } from 'svelte/store'

import { store, project } from 'store'

export const getLayer = (frameId, layerId) => {
  const frame = get(project).frames[frameId]

  return frame.layers[layerId]
}