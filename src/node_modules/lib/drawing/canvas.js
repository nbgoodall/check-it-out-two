import {
  RESOLUTION,
  PIXEL_SIZE,
  DEFAULT_COLORS
} from 'config/constants'

const renderPixels = ({ ctx, pixels }) => {
  for (let x in pixels) {
    for (let y in pixels[x]) {
      let color = pixels[x][y]

      ctx.fillStyle = color

      ctx.fillRect(x * PIXEL_SIZE, y * PIXEL_SIZE, PIXEL_SIZE, PIXEL_SIZE)
    }
  }
}

export {
  renderPixels
}