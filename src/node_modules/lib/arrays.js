export const minMax = (...args) => {
  let arr = args.flat()

  return arr.reduce((minMaxArr, item) => {
    let [min, max] = minMaxArr

    if (item < min) min = item
    if (item > max) max = item

    return [min, max]
  }, [arr[0], arr[0]])
}